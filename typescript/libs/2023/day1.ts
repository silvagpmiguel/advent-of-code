import { Day, NumberArrayInputParts } from '@advent/utils'

export const day1_2023 = ({ part1, part2 }: NumberArrayInputParts, slidingMeasurement: number): Day => {
  return {
    part1: (): number => {
      return 0
    },
    part2: (): number => {
      return 0
    },
  }
}
